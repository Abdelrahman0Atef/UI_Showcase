import 'dart:ui';

class MyTranslations{
  static const translationsPath = 'assets/translations';
  static const supportedLocales = [Locale('en'), Locale('ar')];
  static const arabicTranslations = Locale('ar');
  static const englishTranslations = Locale('en');
}